/** Single topic: factsheet JSON (retained) — list of sensors + capabilities + current thresholds. */
export const OSF_ARDUINO_STATION_FACTSHEET_TOPIC = 'osf/arduino/station/factsheet';

/** Runtime threshold overrides (RAM only on Arduino; not persisted in sketch flash). */
export const OSF_ARDUINO_STATION_CONFIG_TOPIC = 'osf/arduino/station/config';
