#!/usr/bin/env python3
"""
OMF2 UI Constants
Central constants for UI rendering (product SVGs, layout sizes, etc.)
"""

# Product SVG Base Size (in pixels)
# Standard size for product SVG containers
PRODUCT_SVG_BASE_SIZE = 200  # px

# Warehouse Grid Constants
WAREHOUSE_GRID_COLUMNS = 3  # 3x3 grid
WAREHOUSE_CELL_SIZE = 200  # px (same as PRODUCT_SVG_BASE_SIZE)
