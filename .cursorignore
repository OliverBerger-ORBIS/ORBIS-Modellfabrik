# Cursor AI - Ignore Rules
# Diese Dateien sollen NICHT von Cursor AI verändert werden

# Git Hooks - KRITISCH: Nie verändern!
git-hooks/pre-commit

# Wichtige Konfigurationsdateien
.gitignore
requirements.txt
pytest.ini

# Log-Dateien (werden automatisch generiert)
logs/
*.log
*.jsonl

# Temporäre Dateien
*.tmp
*.temp
__pycache__/
*.pyc

# Node modules - verhindert TypeScript Language Server Überlastung
node_modules/
dist/
build/
out/

# Große Session-Dateien (werden nicht für Code-Analyse benötigt)
data/omf-data/sessions/*.log
data/omf-data/sessions/*.db


